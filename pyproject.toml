[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.45.2",
    "docx>=0.2.4",
    "email-validator>=2.2.0",
    "flask-wtf>=1.2.2",
    "flask>=3.1.0",
    "flask-sqlalchemy>=3.1.1",
    "gunicorn>=23.0.0",
    "openai>=1.63.0",
    "pillow>=11.1.0",
    "psycopg2-binary>=2.9.10",
    "pypdf2>=3.0.0",
    "pytesseract>=0.3.13",
    "python-docx>=1.1.2",
    "sqlalchemy>=2.0.38",
    "werkzeug>=3.1.3",
    "flask-login>=0.6.3",
    "reportlab>=4.3.1",
    "requests>=2.32.3",
    "sendgrid>=6.11.0",
    # TTS dependencies below can be removed if audio processing is no longer needed
    "speechrecognition>=3.14.2", # Kept for audio file transcription
    "pydub>=0.25.1", # Kept for audio file processing
    "trafilatura>=2.0.0",
    "langdetect>=1.0.9", # Kept for language detection
    "nltk>=3.9.1", # Kept for text processing
    # Removed TTS functionality, dependency can be removed if not used elsewhere
    "google-cloud-texttospeech>=2.26.0",
    "sift-stack-py>=0.5.2",
    "pocketsphinx>=5.0.4",
]
